import 'package:flutter/foundation.dart';

/// Widget keys for the chat screen.
class ChatKeys {
  static const messageInput = ValueKey('chat_messageInput');
  static const sendButton = ValueKey('chat_sendButton');
  static const messageList = ValueKey('chat_messageList');
  static const newSessionButton = ValueKey('chat_newSessionButton');
}
